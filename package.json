{
  "name": "chief-explorer",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0",
  "homepage": "https://chief.makerdao.com",
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "3.3.0",
    "web3": "^0.20.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && node scripts/copy-cname",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "gulp": "^4.0.2"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
